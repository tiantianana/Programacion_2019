
public interface BFigura
	
	{
	//public abstract: no lo definimos ya que se da por entendido que la interface es publica.
	double getPerimetro();
	
	/*public boolean IsEqual(Figura figura)
	{
		return getPerimetro() == figura.getPerimetro();
	}
	*/

	
	}
